import { Component } from '@angular/core';

@Component({
  selector: 'app-formventas',
  standalone: true,
  imports: [],
  templateUrl: './formventas.component.html',
  styleUrl: './formventas.component.css'
})
export class FormventasComponent {
  /* ID del Usuario (user_id): Identificador del usuario que realiza la venta.
    Productos (products): Una lista de productos que se están vendiendo, cada uno con:
    ID del Producto (product_id): Identificador del producto.
    Cantidad (quantity): Cantidad de unidades vendidas del producto.
    Precio (price): Precio por unidad del producto.
    Total (total): El total de la venta (calculado automáticamente).
    Fecha de la Venta (sale_date): La fecha y hora en que se realiza la venta.  
  */
 //constructor(private form)

}
